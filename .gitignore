# Dependencies (also covered by banking-assistant-extension/.gitignore)
node_modules/

# Claude Code local, machine-specific settings
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Agent skills fetched by modern-web-guidance. skills-lock.json pins them, so
# these are restorable with `npx modern-web-guidance@latest update` and are
# treated like installed dependencies rather than source.
.agents/
.claude/skills/
